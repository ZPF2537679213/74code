三种用于配合排序进行编号的窗口函数
一、rank()
在计算排序序号时，如果遇到重复或相同的值时，会跳过当前位次。
例如：
1、1、1、4等等。
二、dense_rank()
在计算排序序号时，不会跳过当前位次。
例如：
1、1、1、2等等。
三、row_number()
在计算排序时，无论有没有相同值，都按照次序，依次排列。
例如：
1、2、3、4等等。
窗口函数的用法：
窗口函数 over (排序序列)